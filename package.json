{
  "name": "pwa-template",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:gh-pages": "tsc && vite build --base=/lifectl/",
    "serve": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^16.11.56",
    "postcss-load-config": "^3.1.4",
    "postcss-nested": "^5.0.6",
    "typescript": "^4.8.2",
    "vite": "^2.9.13"
  },
  "dependencies": {
    "postcss-import": "^14.1.0"
  }
}
